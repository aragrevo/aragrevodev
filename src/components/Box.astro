---
const {bg = 'bg-blue-100', color = ''} = Astro.props;
---

<div
  class={`text-base capitalize font-medium px-4 py-1.5 shadow-sm rounded flex justify-center items-center gap-1 ${bg} ${color}`}
>
  <slot />
</div>
