---
import Header from '@components/Header.astro';
import '@fontsource/league-spartan';

export interface Props {
  title: string;
}

// const data = {
//   title: 'Aragrevo Dev',
//   text: 'Hello!, This is my profile, thanks for share',
//   url: 'htttps://aragrevp.dev'
// }

// if (navigator.canShare && navigator.canShare(data)) {
//   navigator.share(data)
// }

const {title} = Astro.props;
---

<!DOCTYPE html>
<html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width' />
    <link rel='icon' type='image/svg+xml' href='/favicon.svg' />
    <title>{title}</title>
    <meta name='description' content='Eduardo Vergara Porfolio ' />
    <link href={Astro.url.href} rel='canonical' />
    <meta name='generator' content={Astro.generator} />
  </head>
  <body class='min-h-screen overflow-x-hidden'>
    <Header />
    <slot />
  </body>
</html>
<style is:global>
  html {
    scroll-behavior: smooth;
    font-family: 'League Spartan', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu,
      Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
  }
  html.dark-mode {
    filter: invert(100%);
  }
  /* html.dark-mode img{
    filter: invert(100%)
  } */
</style>
<!-- TODO: Dark mode -->
<!-- TODO: Shared -->
